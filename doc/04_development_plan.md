# 开发计划（按当前实现更新）

> 更新时间：2026-03-06

## Phase A 已完成（基础可用）

目标：完成“可截图可保存”的闭环，并建立新版主界面。

已完成内容：

- 主界面重构（卡片化 + 侧栏 + 顶部操作 + 快速截图 + 最近截图 + 常用设置）
- QSS 主题与 SVG 图标资源接入
- `SelectionOverlay` 选区截图流程
- 全屏截图与区域截图
- 保存 PNG
- `ShowTip` 浮动提示组件

---

## Phase B 近期任务（P0，建议优先）

目标：把“已出现的按钮”从占位改为可用。

任务：

1. 滚动截图：`modeScrollButton`（已完成最小可用版：手动滚动 + 追加帧 + 拼接）
2. 最近截图数据化：`recentGridLayout` 改为读取真实历史
3. 历史入口：`btnHistory` 打开历史面板
4. 常用设置：
   - `btnHotkeySetting`（已接入触发截图，配置能力待实现）
   - `btnFormatSetting`（PNG/JPG/BMP）
   - `btnAutoSave`（自动保存目录）
5. 顶部“新建”：`btnNewTask` 定义明确行为（新建截图会话）

---

## Phase C 截图编辑能力（P1）

目标：将覆盖层工具条按钮接入真实能力。

任务：

1. 画笔工具（自由绘制）
2. 矩形工具
3. 圆形工具
4. 文本工具
5. 马赛克工具
6. 操作栈（撤销/重做）

---

## Phase D 系统能力（P2）

目标：增强桌面端实用性。

任务：

1. 系统托盘
2. 全局快捷键
3. 贴图模式
4. 多屏截图策略与 DPI 适配增强

---

## Phase E 扩展能力（P3）

目标：高阶功能完善。

任务：

1. OCR 文字识别
2. 长截图稳定性优化
3. 历史筛选、搜索、导出

---

## 里程碑验收标准（简要）

- P0 完成：主界面所有核心按钮不再占位
- P1 完成：覆盖层工具条具备真实编辑能力
- P2 完成：具备长期驻留后台使用能力
- P3 完成：具备进阶效率能力（OCR/历史增强）
