# Codex 开发任务清单（按当前 UI 按钮更新）

> 更新时间：2026-03-06
> 说明：本清单以“当前界面已存在按钮”为主线，逐项消除占位接口。

---

## 一、已完成任务

### T001 主界面重构（已完成）

- 左侧导航、顶部工具区、快速截图卡片、最近截图区、右侧设置区
- QSS 主题统一
- SVG 图标资源接入

### T002 基础截图闭环（已完成）

- 全屏截图
- 区域截图
- 保存 PNG

### T003 SelectionOverlay（已完成）

- 选区绘制
- 尺寸提示
- ESC/Enter
- 选区后悬浮工具条

### T004 提示系统（已完成）

- `ShowTip` 替代 `showMessage`

### T101 `modeScrollButton` 滚动截图（已完成）

- 现状：已完成最小可用流程
- 方案：手动滚动 + 多帧追加 + 自动拼接
- 已实现内容：
  - 进入滚动模式后可先框选固定区域
  - 首帧自动抓取
  - 提供“追加一帧 / 完成拼接 / 取消”控制面板
  - 使用重叠行估算算法完成纵向拼接
  - 拼接结果回填预览并可继续保存

---

## 二、按钮驱动待办（P0）

### T102 `btnHistory` 历史面板

- 现状：占位
- 目标：打开历史管理窗口，支持点击预览历史截图

### T103 `btnMoreRecent` 最近截图“更多”

- 现状：占位
- 目标：跳转历史页或展开完整列表

### T104 `btnMoreFormat` 格式“更多”

- 现状：占位
- 目标：打开导出格式配置（PNG/JPG/BMP/WebP 可扩展）

### T105 `btnHotkeySetting` 热键设置

- 现状：已接入“直接触发截图”
- 目标：支持设置并保存全局/应用内快捷键

### T106 `btnFormatSetting` 输出格式设置

- 现状：占位
- 目标：默认格式可配置并持久化

### T107 `btnAutoSave` 自动保存策略

- 现状：仅切换文案
- 目标：接入真实开关、目录与命名规则

### T108 `btnNewTask` 新建截图会话

- 现状：占位
- 目标：定义并实现“清空当前状态 + 进入待截图态”

---

## 三、覆盖层工具条待办（P1）

### T201 画笔工具接入
### T202 矩形工具接入
### T203 圆形工具接入
### T204 文本工具接入
### T205 马赛克工具接入
### T206 撤销/重做（操作栈）

说明：
- 当前工具条操作按钮为：保存 / 取消 / 确认。
- 编辑工具按钮（画笔/矩形/圆形/马赛克/文字）已存在，但未接入真实绘制引擎。

---

## 四、结构优化任务（P1）

### T301 提取 `ScreenCaptureService`

- 将 `MainWindow::captureRegion` 拆为独立服务，降低 UI 类职责。

### T302 提取 `HistoryService`

- 管理历史截图元数据与缩略图。

### T303 提取 `SettingsService`

- 管理热键、格式、自动保存等配置持久化。

---

## 五、系统能力任务（P2）

### T401 托盘功能
### T402 全局快捷键
### T403 贴图模式

---

## 六、执行规则（更新）

1. 每次优先实现一个任务，保证可验证。
2. 新增功能必须绑定到当前 UI 已存在入口。
3. 文档与代码同步更新（至少更新 `04/05/06/07`）。
4. 未实现按钮不得“静默无响应”，必须有明确提示。
5. 所有新增文案和注释使用简体中文。
